## What changed

<!-- One or two sentences. Link the issue if there is one. -->

## How it was tested

- [ ] `backend`: `npm run typecheck && npm test`
- [ ] `frontend`: `npm run lint && npm run build`
- [ ] Checked the change in the browser (desktop and mobile width) if it touches the UI

## Deployment notes

<!-- New env vars, Render/Vercel settings, DB changes, or "none". -->
